services:
  playwright:
    build: .
    environment:
      - WIKI_USERNAME=${WIKI_USERNAME}
      - WIKI_PASSWORD=${WIKI_PASSWORD}
      - CI=true
    volumes:
      # Mount local directory so the HTML report is available on the host after the run
      - ./playwright-report:/app/playwright-report
